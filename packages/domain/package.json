{
  "name": "@bordfodbold/domain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tournament rules as pure functions: schedule generation, score validation, standings.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
